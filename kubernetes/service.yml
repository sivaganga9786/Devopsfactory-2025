apiVersion: v1
kind: Service
metadata:
  name: terraformdemo
  labels:
    app: terraformdemo
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31000
  selector:
    app: terraformdemo